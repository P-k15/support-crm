# Optional: point to a Postgres/MySQL database instead of the default SQLite file.
# If unset, the app uses sqlite:///./crm.db
DATABASE_URL=sqlite:///./crm.db

# Port the app listens on (Railway/Render set this automatically)
PORT=8000
